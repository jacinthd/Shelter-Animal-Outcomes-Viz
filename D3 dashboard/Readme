In a command prompt, make sure you are in the same directory as your D3 code and files.

Python 2 server command: python -m SimpleHTTPServer 8000
Python 3 server command: python3 -m http.server 8000

Then go to http://localhost:8000. After that click on pet_data.html

Otherwise, type http://localhost:8000/pet_data.html to acess the dashboard directly
